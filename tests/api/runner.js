/* eslint-disable */
const { person } = require('./person')
const personModule = require('./person')
const breadModule = require('./bread')
console.info(person)
console.info(personModule)
console.info(breadModule)
console.info(breadModule.data)
